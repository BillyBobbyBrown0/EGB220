This project implements a line-following robotic system using:

DC motors for movement
Infrared (IR) line sensors for path detection
A servo motor for auxiliary control (e.g. steering or mechanism)

The system reads sensor data, determines the robot’s position relative to a line, and dynamically adjusts motor speeds to stay on track.
